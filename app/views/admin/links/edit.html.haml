- content_for :meta_title do
  Edit
  =@cms_config['site_settings']['links_title']

%h1= yield(:meta_title).strip

- form_for [:admin, @link], :html => { :onsubmit => "form_submitted()" } do |f|
  = render :partial => 'form', :locals => { :f => f }

